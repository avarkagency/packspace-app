// The top-right chrome's live keep-out box: `w` in from the right edge, `h` down from the top. Written
// by the widget grid, read by `clampPos` on the drag's hot path. Defaults match a 2-column Balance widget.
export const chromeKeepout = { w: 392, h: 176 }
